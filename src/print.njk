---
title: "ანგულარის ქართული გზამკვლევი"
layout: "base.njk"
description: "ისწავლე Angular-ის ფუნდამენტები ქართულად!"
permalink: "print.html"
---
{%- for article in collections.sortedArticles -%}
    <section class="chapter">
        {{ article.content | safe }}
    </section>
{%- endfor -%}
{# A module script is deferred, so `main.js` has attached its `beforeprint`
   handler by the time this runs — a classic script fires during parsing and
   the page prints in whatever theme is active. #}
<script type="module">
    window.print();
</script>
